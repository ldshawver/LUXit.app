name: Test VPS SSH

on:
  workflow_dispatch:

jobs:
  test-ssh:
    runs-on: ubuntu-latest
    steps:
      - name: Test SSH connection
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: 194.195.92.52
          username: root
          key: ${{ secrets.VPS_SSH_KEY }}
          port: 2222
          timeout: 60s
          command_timeout: 5m
          debug: true
          script_stop: true
          script: |
            echo "CONNECTED"
            whoami
            hostname
            date
