name: Deploy LUXit.app to VPS

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}        # IP ONLY
          username: ${{ secrets.VPS_USER }}    # root or deploy user
          key: ${{ secrets.VPS_SSH_KEY }}      # PRIVATE KEY
          port: 22
          timeout: 30s
          command_timeout: 10m
          script: |
            set -euxo pipefail

            echo "📁 Move to app directory"
            cd /opt/luxit

            echo "📥 Update code"
            git fetch --all
            git reset --hard origin/main

            echo "🐍 Activate venv"
            source venv/bin/activate

            echo "🧪 Compile check"
            python -m py_compile app.py auth.py

            echo "🔁 Restart service"
            systemctl restart lux.service

            echo "🩺 Health check"
            sleep 10
            systemctl is-active --quiet lux.service


            echo "✅ DEPLOY SUCCESS"
